---
description: Git 提交说明必须使用约定式前缀
alwaysApply: true
---

# Git 提交说明

提交说明第一行格式：`type: 简短说明`

只使用下列 type，后面跟中文说明，说明写为什么、不写文件清单：

- `feat:` 新功能、新特性
- `fix:` 修改 bug
- `perf:` 不影响行为的性能优化
- `refactor:` 不影响行为的代码重构
- `docs:` 文档修改
- `style:` 代码格式修改（不是 CSS）
- `test:` 测试用例新增、修改
- `build:` 影响项目构建或依赖项修改
- `revert:` 恢复上一次提交
- `ci:` 持续集成相关文件修改
- `chore:` 其他修改
- `release:` 发布新版本
- `workflow:` 工作流相关文件修改

```text
# 好
feat: 实现任务列表过滤、分页与统计接口

# 坏（无前缀）
实现任务列表过滤分页排序与统计接口。
```

正文可另起一行补充。不要改写已推送到 `main` 的历史，除非用户明确要求 force push。
